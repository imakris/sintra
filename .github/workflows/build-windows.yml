name: Build - Windows

on:
  push:
    branches: [ '*' ]
  pull_request:
    branches: [ '*' ]

jobs:
  build:
    name: Build
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Ensure Windows debugging tools
        shell: pwsh
        run: |
          $debugTools = "${env:ProgramFiles(x86)}\Windows Kits\10\Debuggers\x64"
          if (-not (Test-Path $debugTools)) {
            choco install --no-progress windows-sdk-10-version-10.0.22621.0 --package-parameters "'ADDLOCAL=DebuggingTools'"
          }
          Add-Content -Path $env:GITHUB_PATH -Value $debugTools

      - name: Verify Windows debugger availability
        shell: pwsh
        run: |
          $debuggers = @('cdb', 'ntsd', 'windbg', 'windbgx')
          $found = @()
          foreach ($dbg in $debuggers) {
            $command = Get-Command $dbg -ErrorAction SilentlyContinue
            if ($command) {
              $found += $command.Source
            }
          }
          if (-not $found) {
            throw "No Windows debuggers (cdb, ntsd, windbg, windbgx) found in PATH."
          }
          "Available debuggers:`n$($found -join "`n")"

      - name: Configure CMake
        run: cmake -B build -S . -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: windows-build-${{ github.event.pull_request.head.sha || github.sha }}
          path: build/
          retention-days: 1
