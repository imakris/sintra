# Focused managed-child custody CI observation
#
# Format: <test_path> <iterations>
#   - test_path: relative path to test from tests/ directory
#     (without .cpp extension or sintra_ prefix)
#   - iterations: number of times to run the test (minimum: 1)
#   - Lines starting with # are ignored
#   - Empty lines are ignored
#
# The new causal baseline runs with runnable, extant tests that actually failed
# on this source line during the preceding 72 hours. Intended RED gates and
# infrastructure-only failures are excluded.

managed_child_adverse_cleanup_contract_test              1
